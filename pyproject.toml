[build-system]
requires = [
        "setuptools>=42",
        "scikit-build",
        "cmake>=3.23",
        "wheel",
        "nanobind>=1.2",
        "ninja; platform_system!='Windows'"]
build-backend = "setuptools.build_meta"