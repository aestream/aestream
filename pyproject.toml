[build-system]
requires = [
        "setuptools>=42",
        "scikit-build",
        "cmake>=3.23",
        "wheel",
        "nanobind",
        "ninja; platform_system!='Windows'"]
build-backend = "build_meta"
backend-path = ["scripts"]