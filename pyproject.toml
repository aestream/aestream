[build-system]
requires = [
        "setuptools>=42",
        "scikit-build",
        "cmake>=3.21",
        "wheel",
        "torch>=1.10",
        "ninja; platform_system!='Windows'"]
build-backend = "setuptools.build_meta"
