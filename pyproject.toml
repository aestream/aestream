[build-system]
requires = [
        "setuptools>=42",
        "scikit-build-core",
        "cmake>=3.23",
        "wheel",
        "nanobind>=1.2",
        "ninja; platform_system!='Windows'"]
build-backend = "scikit_build_core.build"

[project]
name = "aestream"
version = "0.6.0"